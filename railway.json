{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "/Dockerfile",
    "watchPatterns": ["/apps/web/**", "/Dockerfile", "/railway.json"]
  },
  "deploy": {
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
